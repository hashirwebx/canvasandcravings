@import url('https://fonts.googleapis.com/css2?family=Bagel+Fat+One&family=Space+Grotesk:wght@400;500;600;700&display=swap');

.cc-root{
  --green:#0F3D1E;
  --cream:#F5EDD8;
  --orange:#E8792E;
  background:var(--green);
  color:var(--cream);
  font-family:'Space Grotesk', sans-serif;
  overflow-x:hidden;
  cursor:none;
  position:relative;
}
.cc-root *{box-sizing:border-box;}
.cc-root a{color:inherit; text-decoration:none;}
.cc-root ::selection{background:var(--orange); color:var(--green);}
.cc-wrap{max-width:1320px; margin:0 auto; padding:0 48px;}

.cc-grain{position:fixed; inset:0; z-index:200; pointer-events:none; opacity:0.05; mix-blend-mode:overlay;}

.cc-cur{position:fixed; top:0; left:0; width:10px; height:10px; border-radius:50%; background:var(--orange); pointer-events:none; z-index:9999; transform:translate(-50%,-50%);}
.cc-cur-ring{position:fixed; top:0; left:0; width:44px; height:44px; border-radius:50%; border:1px solid rgba(245,237,216,0.5); pointer-events:none; z-index:9998; transform:translate(-50%,-50%); display:flex; align-items:center; justify-content:center; font-size:10px; letter-spacing:.08em; text-transform:uppercase; font-weight:600; opacity:0; transition:opacity .2s;}

.cc-pre{position:fixed; inset:0; z-index:500; background:var(--orange); display:flex; align-items:center; justify-content:center;}
.cc-pre span{font-family:'Bagel Fat One'; font-size:20px; color:var(--green);}

.cc-nav{position:fixed; top:0; left:0; right:0; z-index:100; display:flex; align-items:center; justify-content:space-between; padding:26px 48px; mix-blend-mode:difference;}
.cc-nav-mark{font-family:'Bagel Fat One'; font-size:18px; color:var(--cream);}
.cc-nav-right{display:flex; align-items:center; gap:34px; font-size:13px; font-weight:600; text-transform:uppercase; letter-spacing:.06em;}
.cc-nav-right a{opacity:.8;}
.cc-nav-right a:hover{opacity:1;}

.cc-ticker-strip{position:relative; background:var(--orange); overflow:hidden; padding:9px 0; z-index:5;}
.cc-ticker-track{display:flex; gap:40px; width:max-content; white-space:nowrap; font-size:12.5px; font-weight:700; letter-spacing:.08em; text-transform:uppercase; color:var(--green);}
.cc-ticker-track > span{display:flex; gap:40px;}

.cc-hero{min-height:100vh; padding:150px 48px 60px; display:flex; flex-direction:column; justify-content:space-between;}
.cc-hero-top{display:flex; justify-content:space-between; align-items:flex-start; font-size:12.5px; text-transform:uppercase; letter-spacing:.1em; font-weight:600; color:rgba(245,237,216,0.6);}
.cc-hero-top .r{text-align:right;}
.cc-hero-type{margin:20px 0;}
.cc-hero-type .row{display:flex; align-items:center; overflow:hidden;}
.cc-hero-type h1{font-family:'Bagel Fat One'; font-weight:400; font-size:clamp(60px,12vw,168px); line-height:0.92; color:var(--cream); transform:translateY(110%); display:inline-block;}
.cc-hero-type .row:nth-child(2){justify-content:flex-end;}
.cc-hero-type .row:nth-child(2) h1{color:var(--orange);}
.cc-hero-bottom{display:flex; justify-content:space-between; align-items:flex-end; gap:40px; flex-wrap:wrap;}
.cc-hero-bottom p{max-width:340px; font-size:15px; line-height:1.6; color:rgba(245,237,216,0.7); font-weight:500;}
.cc-magnetic{position:relative; width:150px; height:150px; border-radius:50%; border:1px solid var(--cream); display:flex; align-items:center; justify-content:center; text-align:center; font-size:13px; font-weight:700; text-transform:uppercase; letter-spacing:.05em; flex-shrink:0; cursor:none;}

.cc-ritual{background:var(--cream); color:var(--green); padding:60px 0 40px;}
.cc-ritual-row{display:flex; align-items:center; justify-content:space-between; padding:38px 0; border-top:1px solid rgba(15,61,30,0.15); gap:40px;}
.cc-ritual-row:last-child{border-bottom:1px solid rgba(15,61,30,0.15);}
.cc-ritual-num{font-family:'Bagel Fat One'; font-size:16px; color:var(--orange); width:50px; flex-shrink:0;}
.cc-ritual-name{font-family:'Bagel Fat One'; font-size:clamp(28px,4.4vw,58px); flex:1; transition:color .3s ease;}
.cc-ritual-desc{max-width:280px; font-size:14px; color:rgba(15,61,30,0.65); font-weight:500; text-align:right; flex-shrink:0;}
.cc-ritual-row:hover .cc-ritual-name{color:var(--orange);}

.cc-h-gallery{background:var(--green); height:100vh; overflow:hidden; position:relative;}
.cc-h-track{display:flex; height:100%; align-items:center; gap:26px; padding:0 48px; width:max-content;}
.cc-h-card{width:34vw; max-width:440px; height:64vh; border-radius:6px; flex-shrink:0; position:relative; overflow:hidden; cursor:none;}
.cc-h-card::before{content:''; position:absolute; inset:0;}
.cc-h-card.hc1::before{background:linear-gradient(155deg,#1B4A2C,#0F3D1E);}
.cc-h-card.hc2::before{background:linear-gradient(155deg,var(--orange),#B85C1E);}
.cc-h-card.hc3::before{background:linear-gradient(155deg,#0F3D1E,#264F31);}
.cc-h-card.hc4::before{background:linear-gradient(155deg,#C2611F,var(--orange));}
.cc-h-card.hc5::before{background:linear-gradient(155deg,#173F22,#0F3D1E);}
.cc-h-label{position:absolute; bottom:20px; left:20px; z-index:2; font-family:'Bagel Fat One'; font-size:16px; color:var(--cream);}
.cc-h-idx{position:absolute; top:20px; right:20px; z-index:2; font-size:12px; font-weight:700; color:rgba(245,237,216,0.7);}
.cc-h-caption{position:absolute; top:48px; left:48px; z-index:3; font-size:12.5px; text-transform:uppercase; letter-spacing:.1em; font-weight:600; color:rgba(245,237,216,0.5);}

.cc-events{background:var(--cream); color:var(--green); padding:140px 48px;}
.cc-events h2{font-family:'Bagel Fat One'; font-size:clamp(32px,6vw,86px); line-height:1.08; max-width:900px;}
.cc-events h2 .hi{color:var(--orange);}
.cc-events-meta{display:flex; justify-content:space-between; margin-top:60px; padding-top:26px; border-top:1px solid rgba(15,61,30,0.15); font-size:13px; font-weight:600; text-transform:uppercase; letter-spacing:.06em; flex-wrap:wrap; gap:20px;}

.cc-quote{background:var(--green); padding:150px 48px; text-align:center;}
.cc-quote p{font-family:'Bagel Fat One'; font-size:clamp(26px,4.6vw,54px); max-width:920px; margin:0 auto 30px; line-height:1.25; color:var(--cream);}
.cc-quote span{font-size:13px; text-transform:uppercase; letter-spacing:.1em; font-weight:700; color:var(--orange);}

.cc-footer{background:var(--orange); color:var(--green); padding:100px 48px 40px;}
.cc-footer h2{font-family:'Bagel Fat One'; font-size:clamp(40px,9vw,120px); line-height:0.95; margin-bottom:60px;}
.cc-foot-grid{display:flex; justify-content:space-between; flex-wrap:wrap; gap:30px; padding-top:26px; border-top:1px solid rgba(15,61,30,0.25); font-size:13px; font-weight:700; text-transform:uppercase; letter-spacing:.05em;}
.cc-foot-col a{display:block; opacity:.75; margin-bottom:8px;}
.cc-foot-col a:hover{opacity:1;}

@media(max-width:820px){
  .cc-root{cursor:auto;}
  .cc-cur,.cc-cur-ring{display:none;}
  .cc-nav{padding:20px 22px;}
  .cc-hero{padding:120px 22px 40px;}
  .cc-wrap{padding:0 22px;}
  .cc-events,.cc-footer,.cc-ritual{padding-left:22px; padding-right:22px;}
  .cc-events-meta{flex-direction:column;}
  .cc-h-card{width:78vw; cursor:auto;}
  .cc-magnetic{cursor:pointer;}
}
